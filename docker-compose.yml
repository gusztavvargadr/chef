version: '3'

services:
  chef:
    build:
      context: ./build/docker/
      dockerfile: ./chef.Dockerfile
    volumes:
      - ./:/opt/chef/
      - ./.chef-workstation/:/root/.chef-workstation/

  chef-policy:
    build:
      context: ./
      dockerfile: ./build/docker/chef-policy.Dockerfile
      args:
        directory: core
        policy: init
    volumes:
      - ./.chef/:/opt/chef/.chef/

  chef-client-linux:
    build:
      context: ./build/docker/
      dockerfile: ./chef-client-linux.Dockerfile
    volumes:
      - ./:/opt/chef/
        
  chocolatey:
    build:
      context: ./build/docker/
      dockerfile: ./chocolatey.Dockerfile
    volumes:
      - ./:C:/opt/chocolatey/

  chocolatey-package:
    build:
      context: ./
      dockerfile: ./build/docker/chocolatey-package.Dockerfile
      args:
        directory: core
        policy: init
    volumes:
      - ./.chef/:C:/opt/chocolatey/.chef/
      - ./.chocolatey/:C:/opt/chocolatey/.chocolatey/

  chef-client-windows:
    build:
      context: ./build/docker/
      dockerfile: ./chef-client-windows.Dockerfile
    volumes:
      - ./:C:/opt/chef/

