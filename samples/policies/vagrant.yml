machines:
  workstation:
    provisioners:
      chef_policyfile_install:
        paths:
          - workstation/Policyfile.install.rb
  infrastructure:
    provisioners:
      chef_policyfile_install:
        paths:
          - workstation/Policyfile.install.rb
          - development/Policyfile.install.rb
          - infrastructure/Policyfile.install.rb
