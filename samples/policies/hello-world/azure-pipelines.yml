trigger:
  branches:
    include:
      - '*'
  paths:
    include:
      - samples/policies/hello-world
pr: none

stages:
  - template: ../azure-pipelines.stages.yml
    parameters:
      configurations:
        - name: hello-world
          platforms:
            - default-windows
            - default-ubuntu
