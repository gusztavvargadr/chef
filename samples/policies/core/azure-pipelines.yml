trigger:
  branches:
    include:
      - '*'
  paths:
    include:
      - samples/policies/core
pr: none

stages:
  - template: ../../../build/azp/samples/policies/commit.yml
    parameters:
      options:
        - policy: init
          directory: core
        - policy: prepare
          directory: core
