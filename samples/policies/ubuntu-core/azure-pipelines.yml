trigger:
  branches:
    include:
      - '*'
  paths:
    include:
      - samples/policies/ubuntu-core
pr: none

stages:
  - template: ../azure-pipelines.stages.yml
    parameters:
      configurations:
        - name: ubuntu-core
          platforms:
            - default-ubuntu
