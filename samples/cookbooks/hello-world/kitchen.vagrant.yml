driver:
  name: vagrant
  vm_hostname: false
  customize:
    cpus: "2"
    memory: "2048"

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu
    driver:
      box: gusztavvargadr/ubuntu-server

suites:
  - name: default
