trigger: none
pr: none

stages:
  - template: ../../../build/azp/samples/cookbooks/stages.yml
    parameters:
      configurations:
        - cookbook: iis
          driver: docker
          platform: windows
          suites:
            - name: [ '2017', 'community' ]
            - name: [ '2019', 'community' ]
