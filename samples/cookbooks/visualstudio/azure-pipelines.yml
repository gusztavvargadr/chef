trigger: none
pr: none

stages:
  - template: ../../../build/azp/samples/cookbooks/stages.yml
    parameters:
      configurations:
        - cookbook: visualstudio
          driver: vagrant
          platform: windows
          suites:
            - name: [ '2017', 'community' ]
            - name: [ '2019', 'community' ]
