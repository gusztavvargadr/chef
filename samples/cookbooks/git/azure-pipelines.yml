trigger:
  branches:
    include:
      - '*'
  paths:
    include:
      - samples/cookbooks/git
pr: none

stages:
  - template: ../azure-pipelines.stages.yml
    parameters:
      configurations:
        - name: git
          instances:
            - default-windows
            - default-ubuntu-2004
            - default-ubuntu-2204
