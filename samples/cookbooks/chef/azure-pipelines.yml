trigger:
  branches:
    include:
      - '*'
  paths:
    include:
      - samples/cookbooks/chef
pr: none

stages:
  - template: ../azure-pipelines.stages.yml
    parameters:
      configurations:
        - name: chef
          instances:
            - default-windows
            - default-ubuntu-2004
            - default-ubuntu-2204
