driver:
  name: vagrant
  vm_hostname: false
  vagrantfiles:
    - ./kitchen.Vagrantfile

verifier:
  name: inspec

platforms:
  - name: windows
    driver:
      box: gusztavvargadr/windows-server
    transport:
      elevated: true
    provisioner:
      max_retries: 3
      wait_for_retry: 30

  - name: ubuntu
    driver:
      box: gusztavvargadr/ubuntu-server

suites:
  - name: community
    attributes:
      gusztavvargadr_docker:
        engine:
          edition: community
        images:
          hello-world:
    includes:
      - windows
      - ubuntu

  - name: desktop
    attributes:
      gusztavvargadr_docker:
        engine:
          edition: desktop
    includes:
      - windows
