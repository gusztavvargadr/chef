trigger:
  branches:
    include:
      - '*'
  paths:
    include:
      - samples/cookbooks/docker
      - samples/cookbooks/docker_test
pr: none

stages:
  - template: ../../../build/azp/samples/cookbooks/commit.yml
    parameters:
      options:
        - cookbook: docker

  - template: ../../../build/azp/samples/cookbooks/acceptance.yml
    parameters:
      options:
        - cookbook: docker
          name: community
        - cookbook: docker
          name: enterprise
        - cookbook: docker
          name: images
