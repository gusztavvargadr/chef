trigger:
  branches:
    include:
      - '*'
  paths:
    include:
      - samples/cookbooks/hashicorp
pr: none

stages:
  - template: ../azure-pipelines.stages.yml
    parameters:
      configurations:
        - name: hashicorp
          instances:
            - apps-windows
            - apps-ubuntu
