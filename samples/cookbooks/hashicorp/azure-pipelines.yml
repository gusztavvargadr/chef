trigger:
  branches:
    include:
      - '*'
  paths:
    include:
      - samples/cookbooks/hashicorp
pr: none

stages:
  - template: ../azure-pipelines.stages.yml
    parameters:
      configurations:
        - name: hashicorp
          instances:
            - default-windows
            - default-ubuntu-2004
            - default-ubuntu-2204
