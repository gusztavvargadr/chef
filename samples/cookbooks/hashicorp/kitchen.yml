provisioner:
  max_retries: 1
  wait_for_retry: 120

suites:
  - name: default
    attributes:
      gusztavvargadr_hashicorp:
        tools:
          vagrant:latest:
          packer:latest:
          terraform:latest:
          consul-template:latest:
          consul:latest:
          vault:latest:
          nomad:latest:

// split
