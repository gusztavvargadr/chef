trigger:
  branches:
    include:
      - '*'
  paths:
    include:
      - samples/cookbooks/windows
pr: none

stages:
  - template: ../azure-pipelines.stages.yml
    parameters:
      configurations:
        - name: windows
          instances:
            - updates-windows
            - native-packages-windows
