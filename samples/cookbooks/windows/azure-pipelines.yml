pr: none

stages:
  - template: ../../../build/azp/samples/cookbooks/commit.yml
    parameters:
      options:
        - cookbook: windows

  - template: ../../../build/azp/samples/cookbooks/acceptance.yml
    parameters:
      options:
        - cookbook: windows
          suite: chocolatey
        - cookbook: windows
          suite: environment
        - cookbook: windows
          suite: features
        - cookbook: windows
          suite: files
        - cookbook: windows
          suite: native
        - cookbook: windows
          suite: node
        - cookbook: windows
          suite: updates
