parameters:
  configurations: []
    # - cookbook: cookbook
    #   driver: driver
    #   platform: platform
    #   suites: [ 'suite1', 'suite2' ]

stages:
  - template: ../../../build/azp/samples/cookbooks/stages.commit.yml
    parameters:
      configurations: ${{ parameters.configurations }}

  - template: ../../../build/azp/samples/cookbooks/stages.acceptance.yml
    parameters:
      configurations: ${{ parameters.configurations }}
