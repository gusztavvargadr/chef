parameters:
  configurations:
    []
    # - cookbook: cookbook
    #   driver: driver
    #   platform: platform
    #   suites: [ 'suite1', 'suite2' ]

stages:
  - template: ./stages.commit.yml
    parameters:
      configurations: ${{ parameters.configurations }}

  - template: ./stages.acceptance.yml
    parameters:
      configurations: ${{ parameters.configurations }}
