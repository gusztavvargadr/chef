parameters:
  options: []
    # - cookbook: cookbook-name
    #   suite: [ 'suite-name-part-1', 'suite-name-part-2' ]

stages:
  - stage: acceptance
    displayName: Acceptance

    jobs:
      - ${{ each option in parameters.options }}:
        - job: test_${{ option.cookbook }}_${{ join('_', option.suite) }}
          displayName: Test ${{ option.cookbook }} ${{ join('-', option.suite) }}

          pool:
            vmImage: windows-2019

          workspace:
            clean: all

          steps:
            - checkout: self
              submodules: recursive

            - script: |
                docker version
                docker-compose version
                docker container ls -a
                docker image ls -a
              displayName: Init Docker

            - script: |
                choco install chef-workstation --version 0.17.5 --confirm --no-progress
                refreshenv
              displayName: Init Chef Install

            - script: |
                 C:\opscode\chef-workstation\chef-cli.bat --version
              env:
                CHEF_LICENSE: accept-silent
              displayName: Init Chef License

            - script: |
                cd samples/cookbooks/${{ option.cookbook }}_test
                C:\opscode\chef-workstation\kitchen.bat create ${{ join('-', option.suite) }}
              env:
                KITCHEN_YAML: .kitchen.docker.yml
                KITCHEN_PLATFORM_WINDOWS_DOCKER_TAG: ltsc2019
              displayName: Create Kitchen

            - script: |
                cd samples/cookbooks/${{ option.cookbook }}_test
                C:\opscode\chef-workstation\kitchen.bat test ${{ join('-', option.suite) }}
              env:
                KITCHEN_YAML: .kitchen.docker.yml
                KITCHEN_PLATFORM_WINDOWS_DOCKER_TAG: ltsc2019
              displayName: Test Kitchen

            - script: |
                cd samples/cookbooks/${{ option.cookbook }}_test
                C:\opscode\chef-workstation\kitchen.bat destroy ${{ join('-', option.suite) }}
              displayName: Clean Kitchen
              condition: always()
